# 更新日志

## v1.1.6

- 调整保活刷新策略：屏幕亮着时不再立即刷新，改为延后处理——熄屏后自动静默刷新，
  或由 21:30 签到流程一并处理；避免白天出现无解释的页面弹出，用户能直观理解刷新原因
- 说明：v1.1.5 的亮屏即时刷新行为回退（token 日志等改进保留）

## v1.1.5

- 修复保活刷新在亮屏时被跳过的问题：调用失败后无论屏幕状态都会尝试刷新（亮屏会短暂出现页面数秒并自动关闭；保持 30 分钟冷却防频繁打扰）
- 日志区分刷新方式：熄屏为「静默刷新」，亮屏为「刷新（亮屏）」

## v1.1.4

- 保活日志显示具体 token：成功时记录 `token 有效 <token>`，刷新时记录 `旧token → 新token`，便于回溯与排查
- 保活统计文件新增 `token` 字段；`status` 命令的 token 行显示具体值
- 刷新日志统一为 `旧token → 新token` 格式（含超时未获得的情况）

## v1.1.3

- 保活改为逐次记录：每次保活结果都写入日志（含今日成功/失败计数），可完整回溯保活历史
- 新增保活统计文件 `fafu_keepalive.status`；`status` 命令显示最近保活时间与今日成功/失败次数
- 日志超过 256KB 时自动轮转（保留最近 1000 行），避免长期运行无限增长

## v1.1.2

- 新增模块更新检测（`updateJson`）：管理器可自动检测新版本并一键更新

## v1.1.1

- 运行时文件（日志 / 开关 / 签到记录 / PID / 标记 / 配置）统一收进模块目录，不再写入 `/data/adb/` 根目录；随模块卸载一并清除
- 操作按钮改为**服务开关**：点击切换启用 / 停用（即时生效，无需重启）
- 模块描述动态显示开关状态与签到日期（如 `🟢 已启用 · ✅ 09-14 已签到 21:30`），采用绝对日期，避免信息失真
- 新增 `toggle` / `enable` / `disable` 子命令
- 停用语义：停止守护进程，不再进行任何网络请求；开机不自动启动
- 修复：`close_page` 变量名冲突、保活判断改用退出码、保活刷新不唤醒屏幕、`api` 超时自动探测

## v1.1.0

- 修正签到时间描述：主窗口 21:30–22:30，补签时段 22:30–23:00（失败重试覆盖完整补签窗口）

## v1.0.0

- 首个版本：自动签到、白天静默保活、会话失效静默刷新
- KernelSU / Magisk 双平台模块结构（module.prop / customize.sh / service.sh / action.sh / uninstall.sh）
- 全部系统命令 fd 加固；刷新后 `am stack remove` 清理页面，不留残留
